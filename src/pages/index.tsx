import type { NextPage } from "next";

const Home: NextPage = () => {
  return (
    <div>
      <header className="h-screen">
        <img className="w-full h-full" />
      </header>
    </div>
  );
};

export default Home;
